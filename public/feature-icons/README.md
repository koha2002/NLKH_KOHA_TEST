# Logo chức năng tùy chỉnh

Đặt logo PNG, WebP hoặc SVG của chức năng mới vào thư mục này.

Sau đó dùng đường dẫn dạng:

```ts
image: "/feature-icons/ten-logo.png"
```

trong `components/ToolOrbit/orbit.config.ts` hoặc `data/home-products.ts`.
