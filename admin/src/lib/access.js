import{supabase}from"./supabase";export async function getAccess(){const{data:{session}}=await supabase.auth.getSession();if(!session)return{authenticated:false,permissions:[]};const{data,error}=await supabase.rpc("get_my_access");if(error)return{authenticated:true,status:"error",permissions:[],error:error.message};return{permissions:[],...(data||{})}}export function can(a,p){const x=a?.permissions||[];return a?.status==="active"&&(x.includes("*")||x.includes(p))}
