import{caller,hasPermission}from"../_shared/auth.ts";import{json,corsHeaders}from"../_shared/cors.ts";
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders(req)});try{const ctx=await caller(req);if(!(hasPermission(ctx,"seo.manage")||hasPermission(ctx,"site.manage")))return json(req,{error:"Forbidden"},403);const{target}=await req.json();if(target!=="frontend")return json(req,{error:"Unsupported target"},400);const hook=Deno.env.get("RENDER_FRONTEND_DEPLOY_HOOK");if(!hook)return json(req,{error:"Chưa cấu hình RENDER_FRONTEND_DEPLOY_HOOK"},500);const r=await fetch(hook,{method:"POST"});if(!r.ok)throw new Error(`Render hook HTTP ${r.status}`);return json(req,{ok:true,message:"Đã yêu cầu Render build lại frontend."})}catch(e){return json(req,{error:e instanceof Error?e.message:String(e)},500)}})
